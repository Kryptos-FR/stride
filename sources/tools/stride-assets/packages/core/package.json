{
  "name": "stride-assets-core",
  "version": "0.1.0",
  "description": "Core parsing and pattern-matching logic for Stride asset files",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
