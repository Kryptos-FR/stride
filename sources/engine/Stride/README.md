# Stride

Base utility assembly for the Stride game engine. Provides shared image, color, and drawing primitives consumed by the graphics and rendering stacks.

- Image loading and pixel format definitions
- Drawing utilities (lines, rectangles, sprites)
- Shared color and media types used across engine assemblies
