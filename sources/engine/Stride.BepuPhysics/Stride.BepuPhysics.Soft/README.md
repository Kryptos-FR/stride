# Stride.BepuPhysics.Soft

Soft body physics extension for Stride, built on top of `Stride.BepuPhysics`. Simulates deformable objects such as cloth and soft volumes.

- `SoftBodyComponent` — entity component for attaching a soft body simulation to a mesh
- Constraint-based deformation integrated with the Bepu solver
