# Stride.Native

Native interop layer for the Stride game engine. Provides C++ implementations and P/Invoke bindings for platform-specific functionality that cannot be expressed in managed code alone.

- Native audio, graphics, and VR entry points
- Platform-specific memory and performance utilities
- Consumed internally by `Stride.Audio`, `Stride.Graphics`, and `Stride.VirtualReality`
