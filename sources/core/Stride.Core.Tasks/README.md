# Stride.Core.Tasks

Build-time task utility used by the Stride build system. Provides tooling helpers invoked during the MSBuild pipeline.

- Visual Studio / MSBuild installation detection (`LocateDevenv`)
- Used internally by the Stride SDK; not intended for direct use in game projects
